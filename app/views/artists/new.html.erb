<h2>New Artist Form</h2>

<%= form_for(@artist) do |f| %>
  <%= f.label 'Artist Name' %><%= f.text_field :name %>
  <%= f.label 'Artist Name' %><%= f.text_field :name %>
  <%= f.submit 'Create Artist' %>
<% end %>
