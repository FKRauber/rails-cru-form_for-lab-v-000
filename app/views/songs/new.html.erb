<h2>New Song Form</h2>

<%= form_for(@song) do |f| %>
  <%= f.label 'Song Name' %><%= f.text_field :name %>
  <%= f.label 'Song Bio' %><%= f.text_field :bio %>
  <%= f.submit 'Create Song' %>
<% end %>