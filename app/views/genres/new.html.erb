<h2>New Genre Form</h2>

<%= form_for(@genre) do |f| %>
  <%= f.label 'Genre Name' %><%= f.text_field :name %>
  <%= f.label 'Genre Bio' %><%= f.text_field :bio %>
  <%= f.submit 'Create Genre' %>
<% end %>